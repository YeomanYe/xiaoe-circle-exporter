{
  "name": "xiaoe-circle-exporter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "将小鹅通鹅圈子帖子的正文、附件和完整评论打包下载为 ZIP 的 Chrome 扩展和油猴脚本。",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:userscript": "node scripts/build-userscript.mjs",
    "build:all": "npm run build && npm run build:userscript",
    "check": "npm run test && npm run build:all",
    "test": "node --test tests/*.test.js",
    "test:e2e": "npm run build && node tests/extension.e2e.mjs"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "playwright-core": "^1.54.2"
  },
  "engines": {
    "node": ">=20"
  }
}
